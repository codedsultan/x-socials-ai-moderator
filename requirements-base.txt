# ── Web framework ─────────────────────────────────────────────────────────────
fastapi
uvicorn[standard]
pydantic
pydantic-settings
httpx
python-dotenv

# ── Databases ─────────────────────────────────────────────────────────────────
motor
pymongo
sqlalchemy
aiomysql
aiosqlite

# ── Moderation backends ───────────────────────────────────────────────────────
# Anthropic (Claude) — optional; required when MODERATION_MODE=anthropic|hybrid
anthropic

# OpenAI-compatible — covers OpenAI, Groq, Together AI, Mistral, Ollama, LM Studio
# Required when MODERATION_MODE=openai|ollama|hybrid (with hybrid_smart_backend=openai|ollama)
openai

# Local rule-based classifier (detoxify + PyTorch)
# Required when MODERATION_MODE=rule|hybrid
# Weights (~200 MB) are downloaded on first use and cached in ~/.cache/huggingface
# Comment out for a lightweight deployment that uses LLM-only mode
#
# CPU-only torch — installs before detoxify so pip's resolver doesn't pull the
# CUDA build (~2 GB) as a transitive dep. Remove the --index-url line and this
# comment if you ever move to a GPU host.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu
detoxify

# ── Testing ───────────────────────────────────────────────────────────────────
# pytest
# pytest-asyncio